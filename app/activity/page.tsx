import { ArrowDownLeft, ArrowUpRight, Clock3, ExternalLink } from 'lucide-react';
import { AuthGate } from '@/components/auth-gate';
import { AppShell } from '@/components/app-shell';
import { CircleWalletTools } from '@/components/circle-wallet-tools';
export default function Activity(){return <AuthGate><AppShell title="Transactions" eyebrow="History"><div className="toolbar"><input className="search-input" placeholder="Search transactions, addresses, notes…"/><button className="secondary"><Clock3 size={16}/> Filter</button></div><div className="panel"><div className="activity-table"><div className="table-head"><span>Activity</span><span>Network</span><span>Amount</span><span>Status</span><span></span></div>{[{t:'Received',d:'No history yet',a:'+ —',n:'Base',s:'—',i:<ArrowDownLeft size={16}/>},{t:'Sent',d:'To a wallet',a:'- —',n:'Ethereum',s:'—',i:<ArrowUpRight size={16}/>}].map((x,idx)=><div className="table-row" key={idx}><div className="activity-copy"><div className="activity-icon">{x.i}</div><span><b>{x.t}</b><small>{x.d}</small></span></div><span>{x.n}</span><strong>{x.a}</strong><span className="status-chip">{x.s}</span><ExternalLink size={15}/></div>)}</div><div className="empty-state compact"><strong>Transaction history will appear here.</strong><span>Search and filter your activity across every supported chain.</span></div></div><CircleWalletTools surface="activity"/></AppShell></AuthGate>}
