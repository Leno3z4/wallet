import { ArrowRightLeft, Boxes } from 'lucide-react';
import { AuthGate } from '@/components/auth-gate';
import { ToolPage, SelectBox, AmountBox, ActionButton } from '@/components/tool-page';
export default function Bridge(){return <AuthGate><ToolPage title="Bridge" eyebrow="Move across chains" description="Move assets between supported EVM networks."><div className="form-card"><div className="bridge-route"><SelectBox label="From" options={['Ethereum','Base','Arbitrum','Optimism','Polygon']} value="Ethereum" onChange={()=>{}}/><button className="round-button"><ArrowRightLeft size={16}/></button><SelectBox label="To" options={['Base','Ethereum','Arbitrum','Optimism','Polygon']} value="Base" onChange={()=>{}}/></div><SelectBox label="Asset" options={['USDC','ETH','USDT']} value="USDC" onChange={()=>{}}/><AmountBox symbol="USDC"/><div className="route-card"><div><Boxes size={17}/><strong>Best route</strong></div><span>Quote and bridge fee will be shown before signing.</span></div><ActionButton>Review bridge</ActionButton></div></ToolPage></AuthGate>}
