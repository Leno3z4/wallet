'use client';
import { useState } from 'react';
import { ArrowUpRight, Bookmark, Globe, Plus, RefreshCw } from 'lucide-react';
import { AuthGate } from '@/components/auth-gate';
import { AppShell } from '@/components/app-shell';
import { CircleWalletTools } from '@/components/circle-wallet-tools';
export default function Browser(){const [url,setUrl]=useState('https://app.uniswap.org'); const [current,setCurrent]=useState(url); return <AuthGate><AppShell title="Browser" eyebrow="Web3 browser"><div className="browser-bar"><Globe size={17}/><input value={url} onChange={e=>setUrl(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')setCurrent(url)}}/><button onClick={()=>setCurrent(url)}><ArrowUpRight size={16}/></button><button onClick={()=>setCurrent(url)}><RefreshCw size={16}/></button></div><div className="browser-bookmarks"><button><Bookmark size={15}/>Uniswap</button><button><Plus size={15}/>Add site</button></div><div className="browser-frame"><div className="browser-splash"><div className="browser-logo"><Globe size={26}/></div><h2>{current.replace(/^https?:\/\//,'')}</h2><p>Dapp browsing is ready. Paid APIs and services can use x402 / Gateway nanopayments without changing the wallet’s signing model.</p><div className="browser-note">HTTP 402 payments can be handled through the wallet’s stablecoin payment layer.</div></div></div><CircleWalletTools surface="browser"/></AppShell></AuthGate>}
